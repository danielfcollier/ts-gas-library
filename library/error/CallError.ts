// --------------------------------------------------------------------------------------------------
import ErrorTable from './ErrorTable';
// --------------------------------------------------------------------------------------------------
const callError = (error: ErrorTable) => new Error(error);
// --------------------------------------------------------------------------------------------------
export default class CallError {
    // ----------------------------------------------------------------------------------------------
    static calendarApp() {
        return callError(ErrorTable.CalendarApp);
    }
    // ----------------------------------------------------------------------------------------------
    static execution() {
        return callError(ErrorTable.Execution);
    }
    // ----------------------------------------------------------------------------------------------
    static date() {
        return callError(ErrorTable.Date);
    }
    // ----------------------------------------------------------------------------------------------
    static postalCode() {
        return callError(ErrorTable.PostalCode);
    }
    // ----------------------------------------------------------------------------------------------
    static document() {
        return callError(ErrorTable.Document);
    }
    // ----------------------------------------------------------------------------------------------
    static phone() {
        return callError(ErrorTable.Phone);
    }
    // ----------------------------------------------------------------------------------------------
    static orderUpdate() {
        return callError(ErrorTable.OrderUpdate);
    }
    // ----------------------------------------------------------------------------------------------
    static orderRefund() {
        return callError(ErrorTable.OrderRefund);
    }
    // ----------------------------------------------------------------------------------------------
}
// --------------------------------------------------------------------------------------------------