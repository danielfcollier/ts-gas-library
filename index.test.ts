// --------------------------------------------------------------------------------------------------
import { qunit, QUnitParams } from "./__tests__";
import testLibError from "./__tests__/lib.error.test";
// --------------------------------------------------------------------------------------------------
function runQunit() {
  // ------------------------------------------------------------------------------------------------
  const title = 'Test Coverage <QUnitGS2 Test>';
  const verbose = false;
  // ------------------------------------------------------------------------------------------------
  const qunitTestLib = [
    testLibError,
    // test1
  ];
  // ------------------------------------------------------------------------------------------------
  const qunitTestApp = [
    testLibError,
    // test1
  ];
  // ------------------------------------------------------------------------------------------------
  const qunitParams = {
    title,
    verbose,
    tests: [
      ...qunitTestLib,
      ...qunitTestApp
    ]
  };
  // ------------------------------------------------------------------------------------------------
  qunit(qunitParams);
  // ------------------------------------------------------------------------------------------------
}
// --------------------------------------------------------------------------------------------------